org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true

# TODO(RELEASE): Replace this placeholder with the institution-owned reverse
# domain identifier before any release build. Release tasks fail while it is
# left under com.example.*.
NUTRISENSE_APPLICATION_ID=com.example.nutrisense
